<argList>
	<arg name="regex" 
		brief="-r.r"
		long="--r.regex"
		args="location [location ...]"
		class="java.lang.String"
		default=""
		min="1" 
		max="Integer.MAX_VALUE"
		methodName="parseRegex"
		>
		<help>
		
			REGEX:
			Reads location(s) of the source of regex files. These can be :
			- files within the distribution (""regex/...")
			- files on local files store
			- URLs 
			Any number of files can be imported. The at present they are sequentially OR'ed, i.e.
			each regex independently annotates/indexes the document.
			
		</help>
	</arg>	
	
</argList>
			